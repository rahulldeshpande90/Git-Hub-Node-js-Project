name: Secure DevSecOps Pipeline

on:
  push:
    branches:
      - master

jobs:
  build_and_package:
    name: BUILD-JOB
    runs-on: ubuntu-slim


    steps:
      - name: Checkout Code
        uses: actions/checkout@v4


      - name: BUILD
        uses: npm ci

      - name: TEST
        uses: echo code is tested !!!

